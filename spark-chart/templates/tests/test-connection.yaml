apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spark-chart.fullname" . }}-test-connection"
  labels:
    {{- include "spark-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spark-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
