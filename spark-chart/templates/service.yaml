apiVersion: v1
kind: Service
metadata:
  name: {{ include "spark-chart.fullname" . }}
  labels:
    {{- include "spark-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spark-chart.selectorLabels" . | nindent 4 }}
