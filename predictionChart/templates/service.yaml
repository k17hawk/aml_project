apiVersion: v1
kind: Service
metadata:
  name: {{ include "predictionChart.fullname" . }}
  labels:
    {{- include "predictionChart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "predictionChart.selectorLabels" . | nindent 4 }}
