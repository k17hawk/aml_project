apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "predictionChart.fullname" . }}-test-connection"
  labels:
    {{- include "predictionChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "predictionChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
